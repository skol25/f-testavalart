// Root paths for different sections
const ROOTS = {
  HOME: "/",
};

// Path configuration object
export const paths = {
  // Home section paths
  home: {
    root: `${ROOTS.HOME}`,
    features: `${ROOTS.HOME}/features`,
    // Add more paths as needed for the home section
  },
  // Add more sections with their respective paths as needed
};
